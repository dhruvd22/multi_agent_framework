# Koyeb deployment configuration

[build]
buildCommand = "cd backend && pip install -r requirements.txt"
runCommand = "cd backend && uvicorn api.main:app --host 0.0.0.0 --port $PORT"

[deploy]
healthcheckPath = "/health"
healthcheckInterval = 30

